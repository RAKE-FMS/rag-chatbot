# chat-web
chat-api を呼び出すためのフロントエンドアプリケーション。

## ローカル開発
- 環境変数を設定する。
     - `.env.example` をコピーして `.env` を作成する。
- 依存パッケージをインストールする。
    ```bash
    npm install
    ```
- 開発サーバーを起動する。
    ```bash
    npm run dev
    ```
- リントとフォーマットを実行する。
    ```bash
    npm run lint
    npm run format
    ```